# Core
torch>=2.0.0
torch-geometric>=2.3.0
torch-scatter>=2.1.0
torch-cluster>=1.6.0
pytorch-lightning>=2.0.0

# Quantum — simulation (always needed)
pennylane>=0.35.0
pennylane-lightning>=0.35.0         # faster CPU simulator

# Quantum — GPU simulation (install if CUDA available)
# pennylane-lightning[gpu]          # uncomment for GPU quantum sim

# Quantum — IBM hardware
pennylane-qiskit>=0.35.0
qiskit>=1.0.0
qiskit-aer>=0.14.0
qiskit-ibm-runtime>=0.20.0

# ML utilities
scikit-learn>=1.3.0
numpy>=1.24.0

# Experiment tracking
wandb>=0.16.0
tensorboard>=2.14.0

# Config & I/O
pyyaml>=6.0
pyarrow>=14.0.0

# Optional
rich>=13.0.0
